# Инструкции по тестированию системы логирования

## Шаг 1: Проверка установки

### 1.1. Убедитесь, что все файлы на месте

Проверьте наличие следующих файлов:

```
✅ core/settings.py (изменен - добавлена конфигурация логирования)
✅ rental/logging_utils.py (новый)
✅ rental/middleware.py (новый)
✅ rental/logging_examples.py (новый)
✅ rental/test_logging.py (новый)
✅ logs/.gitignore (новый)
✅ LOGGING_QUICKSTART.md (новый)
✅ LOGGING_GUIDE.md (новый)
✅ LOGGING_SETUP_SUMMARY.md (новый)
✅ TEST_LOGGING_INSTRUCTIONS.md (этот файл)
```

### 1.2. Проверьте middleware в settings.py

Откройте `core/settings.py` и найдите список `MIDDLEWARE`. Убедитесь, что там есть:

```python
'rental.middleware.RequestLoggingMiddleware',  # Логирование запросов
```

## Шаг 2: Запуск автоматического теста

### Windows PowerShell:

```powershell
# Перейдите в корневую папку проекта
cd D:\cursor\batttery3\batteryRental-3

# Запустите тест
python manage.py shell
```

В открывшейся оболочке выполните:

```python
from rental.test_logging import test_logging
test_logging()
```

### Ожидаемый результат:

```
============================================================
ТЕСТИРОВАНИЕ СИСТЕМЫ ЛОГИРОВАНИЯ
============================================================

1. Проверка папки логов...
   ✅ Папка логов существует: D:\cursor\batttery3\batteryRental-3\logs

2. Тестирование log_info()...
   ✅ log_info() работает

3. Тестирование log_debug()...
   ✅ log_debug() работает

4. Тестирование log_warning()...
   ✅ log_warning() работает

5. Тестирование log_error()...
   ✅ log_error() работает

6. Тестирование log_action()...
   ✅ log_action() работает

7. Тестирование LogOperation контекстного менеджера...
   ✅ LogOperation работает

8. Тестирование LogOperation с исключением...
   ✅ LogOperation корректно обрабатывает исключения

9. Проверка созданных файлов логов...
   ✅ general.log существует (XXX байт)
   ✅ errors.log существует (XXX байт)
   ✅ actions.log существует (XXX байт)

============================================================
ТЕСТИРОВАНИЕ ЗАВЕРШЕНО
============================================================
```

## Шаг 3: Запуск сервера и проверка автоматического логирования

### 3.1. Запустите сервер:

```powershell
python manage.py runserver
```

### 3.2. Откройте браузер и зайдите в админку:

```
http://127.0.0.1:8000/admin/
```

### 3.3. Выполните какое-нибудь действие:

- Создайте новый платеж
- Обновите финансового партнёра
- Создайте вывод владельца

### 3.4. Проверьте логи:

Откройте новое окно PowerShell и выполните:

```powershell
# Просмотр последних 20 строк общего лога
Get-Content logs\general.log -Tail 20

# Просмотр последних 20 строк логов действий
Get-Content logs\actions.log -Tail 20

# Просмотр логов в реальном времени
Get-Content logs\general.log -Wait
```

### Что вы должны увидеть:

#### 1. HTTP запросы (автоматически логируются middleware):

```
2026-01-06 15:30:25 [INFO] rental.actions - ДЕЙСТВИЕ: POST /admin/rental/payment/add/ | Статус: 302 | Время: 0.15с | IP: 127.0.0.1 | Пользователь: Админ (ID: 1)
```

#### 2. Действия из кода (если вы использовали наши примеры):

```
2026-01-06 15:30:25 [INFO] rental.actions - Создан финансовый партнёр | Пользователь: Админ (ID: 1) | IP: 127.0.0.1 | partner_id: 5 | user: John Doe | role: Модератор
```

#### 3. Предупреждения:

```
2026-01-06 15:30:25 [WARNING] rental - ПРЕДУПРЕЖДЕНИЕ: Вывод крупной суммы | Пользователь: Админ (ID: 1) | IP: 127.0.0.1 | Контекст: withdrawal_id: 10 | amount: 15000.0
```

## Шаг 4: Проверка логирования ошибок

### 4.1. Создайте намеренную ошибку:

В админке попробуйте создать финансового партнёра-модератора БЕЗ указания города (если настроена валидация).

### 4.2. Проверьте лог ошибок:

```powershell
Get-Content logs\errors.log -Tail 30
```

### Ожидаемый результат:

```
2026-01-06 15:30:25 [ERROR] rental - ОШИБКА: Ошибка валидации финансового партнёра
Пользователь: Админ (ID: 1)
Запрос: POST /admin/rental/financepartner/add/ | IP: 127.0.0.1
Контекст: partner_id: новый | user: John Doe | role: MODERATOR
Исключение: ValidationError: Модератор должен быть привязан к городу
```

## Шаг 5: Проверка структуры папки logs

```powershell
# Посмотрите содержимое папки logs
Get-ChildItem logs\
```

Должны быть файлы:
```
.gitignore
general.log
errors.log
actions.log
sql.log (только если DEBUG=True)
```

## Шаг 6: Интеграция в ваш код

### 6.1. Откройте файл, где хотите добавить логирование

Например, `rental/admin.py` или `rental/views.py`

### 6.2. Импортируйте нужные функции:

```python
from .logging_utils import log_action, log_error, log_warning, log_info
```

### 6.3. Добавьте логирование:

#### Пример для Admin:

```python
class MyModelAdmin(admin.ModelAdmin):
    def save_model(self, request, obj, form, change):
        try:
            super().save_model(request, obj, form, change)
            
            log_action(
                "Создан/обновлён объект",
                user=request.user,
                details={'id': obj.id, 'name': str(obj)},
                request=request
            )
            
        except Exception as e:
            log_error(
                "Ошибка при сохранении",
                exception=e,
                user=request.user,
                request=request
            )
            raise
```

#### Пример для View:

```python
def my_view(request):
    try:
        # Ваш код
        result = do_something()
        
        log_action(
            "Выполнено действие",
            user=request.user,
            details={'result': result},
            request=request
        )
        
        return JsonResponse({'success': True})
        
    except Exception as e:
        log_error(
            "Ошибка в представлении",
            exception=e,
            user=request.user,
            request=request
        )
        return JsonResponse({'error': str(e)}, status=500)
```

## Шаг 7: Мониторинг логов в production

### Рекомендуемые команды для мониторинга:

```powershell
# Следить за логами в реальном времени
Get-Content logs\general.log -Wait

# Последние 50 строк
Get-Content logs\general.log -Tail 50

# Поиск ошибок
Select-String -Pattern "ERROR" -Path logs\general.log

# Поиск действий конкретного пользователя
Select-String -Pattern "Иван" -Path logs\actions.log

# Медленные запросы
Select-String -Pattern "МЕДЛЕННЫЙ" -Path logs\general.log

# Количество ошибок за сегодня
Select-String -Pattern "ERROR" -Path logs\general.log | Measure-Object -Line
```

### Linux/Mac команды:

```bash
# Следить за логами
tail -f logs/general.log

# Последние 50 строк
tail -50 logs/general.log

# Поиск ошибок
grep "ERROR" logs/general.log

# Количество ошибок
grep -c "ERROR" logs/general.log
```

## Troubleshooting

### Проблема: Папка logs не создается

**Решение:**
```powershell
# Создайте папку вручную
mkdir logs
```

### Проблема: Логи не пишутся

**Решение:**
1. Проверьте права доступа к папке logs
2. Проверьте настройки в `core/settings.py`
3. Убедитесь, что DEBUG=True для тестирования
4. Перезапустите сервер

### Проблема: Middleware не работает

**Решение:**
1. Убедитесь, что `rental.middleware.RequestLoggingMiddleware` добавлен в MIDDLEWARE
2. Проверьте, что он идет ПОСЛЕ `django.contrib.auth.middleware.AuthenticationMiddleware`
3. Перезапустите сервер

### Проблема: Ошибка импорта

**Решение:**
```python
# Убедитесь, что импортируете из правильного места
from rental.logging_utils import log_action  # ✅ Правильно
from logging_utils import log_action  # ❌ Неправильно
```

## Полезные ссылки

- **Быстрый старт:** `LOGGING_QUICKSTART.md`
- **Полное руководство:** `LOGGING_GUIDE.md`
- **Примеры кода:** `rental/logging_examples.py`
- **Сводка изменений:** `LOGGING_SETUP_SUMMARY.md`

## Следующие шаги

После успешного тестирования:

1. ✅ Изучите `LOGGING_QUICKSTART.md`
2. ✅ Посмотрите примеры в `rental/logging_examples.py`
3. ✅ Добавьте логирование в критичные места вашего кода
4. ✅ Настройте регулярную проверку `errors.log`
5. ✅ При необходимости настройте алерты на ошибки

---

## Готово! 🎉

Система логирования настроена и работает. Теперь вы можете отслеживать все важные события, ошибки и действия пользователей в вашем приложении.

**При возникновении вопросов обращайтесь к документации в файлах LOGGING_*.md**

