# Система логирования - Готова к использованию! 🎉

## Что было сделано

Я настроил для вас **полноценную систему логирования**, которая поможет отслеживать все важные события, ошибки и действия пользователей в вашем приложении аренды батарей.

## ✅ Что работает прямо сейчас

### Автоматическое логирование:
- **Все HTTP запросы** с информацией о пользователе, IP, времени выполнения
- **Медленные запросы** (> 3 секунд)
- **Все ошибки** сервера (5xx) и клиента (4xx)
- **Все действия изменения данных** (POST/PUT/DELETE/PATCH)

### Файлы логов:
- `logs/general.log` - общие логи приложения
- `logs/errors.log` - только ошибки с подробностями
- `logs/actions.log` - действия пользователей
- `logs/sql.log` - SQL запросы (только в режиме DEBUG)

### Интеграция в код:
- Примеры добавлены в `rental/admin.py` (FinancePartnerAdmin, OwnerWithdrawalAdmin)
- Примеры добавлены в `rental/views.py` (функция dashboard)
- Middleware автоматически логирует все запросы

## 🚀 Быстрый старт

### 1. Запустите тест системы:

```powershell
# Откройте PowerShell в корне проекта
python manage.py shell
```

В оболочке выполните:
```python
from rental.test_logging import test_logging
test_logging()
```

Вы должны увидеть все зелёные галочки ✅

### 2. Запустите сервер и проверьте логи:

```powershell
# Терминал 1: Запуск сервера
python manage.py runserver

# Терминал 2: Просмотр логов в реальном времени
Get-Content logs\general.log -Wait
```

### 3. Выполните действие в админке:

- Зайдите в админку: http://127.0.0.1:8000/admin/
- Создайте платеж или обновите финансового партнёра
- Посмотрите, как это отобразилось в логах

## 📝 Как использовать в своём коде

### Импорт:

```python
from rental.logging_utils import log_action, log_error, log_warning, log_info
```

### Логирование действий пользователя:

```python
log_action(
    "Создан новый платеж",
    user=request.user,
    details={
        'payment_id': payment.id,
        'amount': 500.00,
        'type': 'RENT'
    },
    request=request
)
```

### Логирование ошибок:

```python
try:
    # Ваш код
    risky_operation()
except Exception as e:
    log_error(
        "Ошибка при операции",
        exception=e,
        user=request.user,
        context={'operation': 'название'},
        request=request
    )
```

### Логирование предупреждений:

```python
if amount > 10000:
    log_warning(
        "Крупная сумма операции",
        user=request.user,
        context={'amount': amount},
        request=request
    )
```

## 📊 Примеры из логов

### HTTP запрос:
```
2026-01-06 15:30:25 [INFO] rental.actions - ДЕЙСТВИЕ: POST /admin/rental/payment/add/ | Статус: 302 | Время: 0.15с | IP: 127.0.0.1 | Пользователь: Иван Иванов (ID: 1)
```

### Действие пользователя:
```
2026-01-06 15:30:25 [INFO] rental.actions - Создан финансовый партнёр | Пользователь: Админ (ID: 1) | IP: 127.0.0.1 | partner_id: 5 | user: Виталий | role: Модератор | city: Варшава
```

### Ошибка:
```
2026-01-06 15:30:25 [ERROR] rental - ОШИБКА: Ошибка валидации финансового партнёра
Пользователь: Админ (ID: 1)
Запрос: POST /admin/rental/financepartner/5/change/ | IP: 127.0.0.1
Контекст: partner_id: 5 | role: MODERATOR
Исключение: ValidationError: Модератор должен быть привязан к городу

Трейсбек:
...
```

## 📁 Созданные файлы

### Новые файлы:
- `rental/logging_utils.py` - утилиты для логирования
- `rental/middleware.py` - автоматическое логирование запросов
- `rental/logging_examples.py` - 8 примеров использования
- `rental/test_logging.py` - тестовый скрипт
- `logs/.gitignore` - логи не попадут в git

### Документация:
- `LOGGING_QUICKSTART.md` - быстрое руководство (начните с этого!)
- `LOGGING_GUIDE.md` - полное руководство с примерами
- `LOGGING_SETUP_SUMMARY.md` - сводка всех изменений
- `TEST_LOGGING_INSTRUCTIONS.md` - инструкции по тестированию
- `README_LOGGING_RU.md` - этот файл

### Изменённые файлы:
- `core/settings.py` - добавлена конфигурация логирования
- `rental/admin.py` - добавлены примеры логирования
- `rental/views.py` - добавлены примеры логирования

## 🔍 Просмотр логов

### Windows PowerShell:

```powershell
# Последние 20 строк
Get-Content logs\general.log -Tail 20
Get-Content logs\errors.log -Tail 20
Get-Content logs\actions.log -Tail 20

# В реальном времени
Get-Content logs\general.log -Wait

# Поиск ошибок
Select-String -Pattern "ERROR" -Path logs\general.log

# Поиск по пользователю
Select-String -Pattern "Иван" -Path logs\actions.log

# Медленные запросы
Select-String -Pattern "МЕДЛЕННЫЙ" -Path logs\general.log
```

## 💡 Рекомендации по использованию

### ✅ Что логировать:
- Создание/обновление/удаление важных объектов (платежи, аренда, батареи)
- Все ошибки и исключения
- Изменения статусов
- Финансовые операции
- Необычные ситуации (крупные суммы, долгая аренда и т.д.)

### ❌ Что НЕ логировать:
- Пароли и токены
- Персональные данные в открытом виде
- Слишком много записей в циклах (тысячи записей)
- Каждый GET запрос (это делает middleware автоматически)

## 🎯 Уровни логирования

| Функция | Использование | Файл |
|---------|--------------|------|
| `log_debug()` | Отладка (не пишется в production) | general.log |
| `log_info()` | Информация о процессах | general.log |
| `log_warning()` | Предупреждения | general.log |
| `log_error()` | Ошибки и исключения | errors.log, general.log |
| `log_action()` | Действия пользователей | actions.log, general.log |

## 🔧 Настройки

### DEBUG режим (разработка):
- Все логи пишутся
- SQL запросы логируются
- DEBUG сообщения отображаются

### Production режим:
- DEBUG логи не пишутся
- SQL запросы не логируются
- Только важные сообщения (INFO и выше)

## 📚 Полная документация

1. **Быстрый старт:** `LOGGING_QUICKSTART.md` - начните с этого!
2. **Полное руководство:** `LOGGING_GUIDE.md` - все возможности
3. **Примеры кода:** `rental/logging_examples.py` - 8 подробных примеров
4. **Тестирование:** `TEST_LOGGING_INSTRUCTIONS.md` - как протестировать

## 🎓 Примеры интеграции

### В Django Admin (save_model):

```python
def save_model(self, request, obj, form, change):
    from .logging_utils import log_action, log_error
    
    try:
        super().save_model(request, obj, form, change)
        
        log_action(
            "Создан/обновлён объект",
            user=request.user,
            details={'id': obj.id, 'name': str(obj)},
            request=request
        )
        
    except Exception as e:
        log_error(
            "Ошибка при сохранении",
            exception=e,
            user=request.user,
            request=request
        )
        raise
```

### В Views:

```python
def my_view(request):
    from .logging_utils import log_action, log_error
    
    try:
        # Ваша логика
        result = process_data()
        
        log_action(
            "Обработка завершена",
            user=request.user,
            details={'result': result},
            request=request
        )
        
        return JsonResponse({'success': True})
        
    except Exception as e:
        log_error(
            "Ошибка обработки",
            exception=e,
            user=request.user,
            request=request
        )
        return JsonResponse({'error': str(e)}, status=500)
```

### Контекстный менеджер для операций:

```python
from .logging_utils import LogOperation

with LogOperation("Массовое обновление", user=request.user) as op:
    updated = 0
    for item in items:
        item.update()
        updated += 1
    
    op.add_result({'updated': updated})
    # Автоматически залогируется начало, конец и результат
    # Если произойдёт ошибка - она тоже залогируется
```

## 🔥 Готово к использованию!

Система полностью настроена и работает. Вы можете:

1. ✅ Сразу видеть все HTTP запросы в логах (автоматически)
2. ✅ Добавлять своё логирование в любое место кода
3. ✅ Отслеживать ошибки и действия пользователей
4. ✅ Анализировать производительность (медленные запросы)

## 🆘 Помощь

Если что-то не работает:
1. Прочитайте `TEST_LOGGING_INSTRUCTIONS.md`
2. Запустите тест: `python manage.py shell` → `from rental.test_logging import test_logging; test_logging()`
3. Проверьте, что папка `logs/` существует
4. Посмотрите примеры в `rental/logging_examples.py`

## 🎯 Следующие шаги

1. Запустите тест логирования (см. "Быстрый старт" выше)
2. Изучите `LOGGING_QUICKSTART.md`
3. Посмотрите примеры в `rental/logging_examples.py`
4. Добавьте логирование в критичные места вашего кода
5. Регулярно проверяйте `logs/errors.log` на наличие проблем

---

**Автор:** AI Assistant  
**Дата:** 2026-01-06  
**Статус:** ✅ Готово к использованию

**Удачи в отладке вашей системы!** 🚀

