# Быстрый старт системы логирования

## Что уже настроено

✅ Система логирования полностью настроена и готова к использованию  
✅ Middleware автоматически логирует все HTTP запросы  
✅ Логи записываются в папку `logs/`  
✅ Примеры интеграции добавлены в код  

## Структура логов

- `logs/general.log` - Общие логи приложения
- `logs/errors.log` - Только ошибки (с трейсбеками)
- `logs/actions.log` - Действия пользователей
- `logs/sql.log` - SQL запросы (только в DEBUG режиме)

## Как начать использовать

### 1. Импортируйте утилиты в нужный файл

```python
from rental.logging_utils import log_action, log_error, log_warning, log_info
```

### 2. Логируйте важные действия

```python
# В admin.py при сохранении
log_action(
    "Создан платеж",
    user=request.user,
    details={'payment_id': payment.id, 'amount': float(payment.amount)},
    request=request
)
```

### 3. Логируйте ошибки

```python
try:
    risky_operation()
except Exception as e:
    log_error(
        "Ошибка при операции",
        exception=e,
        user=request.user,
        context={'operation': 'название'},
        request=request
    )
```

## Примеры уже добавлены

Посмотрите реальные примеры интеграции в:
- `rental/admin.py` - класс `FinancePartnerAdmin`, `OwnerWithdrawalAdmin`
- `rental/views.py` - функция `dashboard`

## Просмотр логов

### Windows PowerShell
```powershell
# Смотреть логи в реальном времени
Get-Content logs\general.log -Wait
Get-Content logs\errors.log -Wait
Get-Content logs\actions.log -Wait

# Просмотр всего файла
Get-Content logs\general.log

# Поиск ошибок
Select-String -Pattern "ERROR" -Path logs\general.log
```

### Linux/Mac
```bash
# Смотреть логи в реальном времени
tail -f logs/general.log
tail -f logs/errors.log
tail -f logs/actions.log

# Поиск ошибок
grep "ERROR" logs/general.log
```

## Что логируется автоматически

### HTTP запросы (через Middleware)

- ✅ Все POST/PUT/DELETE/PATCH запросы
- ✅ Медленные запросы (> 3 секунд)
- ✅ Ошибки 5xx и 4xx
- ✅ IP адреса пользователей
- ✅ Время обработки каждого запроса

Пример из логов:
```
2026-01-06 15:30:25 [INFO] rental.actions - ДЕЙСТВИЕ: POST /admin/rental/payment/add/ | Статус: 302 | Время: 0.15с | IP: 192.168.1.1 | Пользователь: Иван Иванов (ID: 1)
```

## Быстрая справка по функциям

| Функция | Когда использовать |
|---------|-------------------|
| `log_action()` | Важные действия пользователей (создание платежа, изменение статуса) |
| `log_error()` | Ошибки и исключения |
| `log_warning()` | Предупреждения (долгая аренда, крупная сумма) |
| `log_info()` | Информация о процессах (запуск синхронизации) |
| `log_debug()` | Отладка (только в DEBUG режиме) |

## Рекомендации

### ✅ Логируйте:
- Создание/обновление/удаление важных объектов (платежи, аренда)
- Все ошибки с контекстом
- Изменения статусов
- Финансовые операции
- Необычные ситуации

### ❌ Не логируйте:
- Пароли и токены
- Слишком много в циклах
- Чувствительные данные
- Каждый GET запрос (это делает middleware)

## Уровни логирования

```python
# DEBUG - только для разработки (не пишется в production)
log_debug("Детальная отладка", details={'var': value})

# INFO - обычная информация
log_info("Процесс завершен", details={'count': 100})

# WARNING - предупреждения
log_warning("Батарея в аренде 90+ дней", context={'battery_id': 123})

# ERROR - ошибки
log_error("Критическая ошибка", exception=e, user=user)

# ACTION - действия пользователей (пишется в actions.log)
log_action("Создан платеж", user=user, details={'id': 123})
```

## Полная документация

Подробная документация: `LOGGING_GUIDE.md`  
Примеры кода: `rental/logging_examples.py`

## Тестирование

Запустите сервер и выполните любое действие в админке (создайте платеж, обновите партнёра и т.д.), затем проверьте логи:

```powershell
# Windows
Get-Content logs\actions.log -Tail 20
Get-Content logs\general.log -Tail 20
```

Вы увидите:
- Автоматические логи от middleware (HTTP запросы)
- Ваши логи действий (если добавили в код)
- Логи из примеров (FinancePartnerAdmin, OwnerWithdrawalAdmin)

## Поддержка

Если что-то не работает:
1. Убедитесь, что папка `logs/` существует (создается автоматически)
2. Проверьте права доступа к папке `logs/`
3. Посмотрите примеры в `rental/admin.py` и `rental/views.py`
4. Изучите полную документацию в `LOGGING_GUIDE.md`

---

**Готово к использованию!** 🎉

Просто импортируйте нужные функции и начните логировать важные события в вашем приложении.

