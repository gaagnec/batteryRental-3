# Инструкции по тестированию обработки ошибок

## Быстрая проверка

### Тест 1: Успешное создание объекта

1. Откройте админку: http://127.0.0.1:8000/admin/
2. Перейдите в раздел "Payments" (Платежи)
3. Создайте новый платёж с корректными данными
4. **Ожидаемый результат:**
   - ✅ Зелёное сообщение: "Создан новый платёж успешно (ID: X, сумма: Y PLN)"
   - ✅ Платёж появился в списке
   - ✅ Запись в `logs/actions.log`

### Тест 2: Ошибка валидации

1. Перейдите в раздел "Finance Partners" (Финансовые партнёры)
2. Попробуйте создать модератора БЕЗ указания города
3. **Ожидаемый результат:**
   - ❌ Красное сообщение с описанием ошибки
   - ❌ Партнёр НЕ создан
   - ✅ Запись в `logs/errors.log` (без traceback)

### Тест 3: Крупная сумма (предупреждение)

1. Перейдите в раздел "Payments"
2. Создайте платёж с суммой > 5000 PLN
3. **Ожидаемый результат:**
   - ✅ Зелёное сообщение об успехе
   - ✅ Платёж создан
   - ⚠️ Запись WARNING в `logs/general.log` о крупной сумме

### Тест 4: Страница 404

1. Откройте несуществующую страницу: http://127.0.0.1:8000/admin/nonexistent/
2. **Ожидаемый результат:**
   - 🎨 Красивая страница "404 - Страница не найдена"
   - 📝 Предложения что делать
   - 🔗 Кнопки "На главную" и "Назад"

### Тест 5: Страница 403 (доступ запрещён)

1. Выйдите из админки (Logout)
2. Попробуйте открыть: http://127.0.0.1:8000/admin/rental/payment/
3. **Ожидаемый результат:**
   - 🎨 Красивая страница "403 - Доступ запрещён"
   - 📝 Объяснение причин
   - 🔗 Кнопка "Вернуться на главную"

## Проверка логов

### После тестов проверьте логи:

```powershell
# Windows PowerShell

# Последние записи в логе действий
Get-Content logs\actions.log -Tail 20

# Последние ошибки
Get-Content logs\errors.log -Tail 20

# Все логи вместе
Get-Content logs\general.log -Tail 30
```

## Что должно быть в логах

### actions.log:
```
2026-01-06 15:30:25 [INFO] rental.actions - Создан новый платёж | Пользователь: Админ (ID: 1) | payment_id: 123 | amount: 500.0 | type: Аренда
2026-01-06 15:30:26 [INFO] rental.actions - ДЕЙСТВИЕ: POST /admin/rental/payment/add/ | Статус: 302 | Время: 0.15с
```

### errors.log (при ошибке валидации):
```
2026-01-06 15:30:30 [ERROR] rental - ОШИБКА: Ошибка валидации финансового партнёра
Пользователь: Админ (ID: 1)
Контекст: partner_id: новый | role: MODERATOR
Исключение: ValidationError: Модератор должен быть привязан к городу
```

### general.log (предупреждение о крупной сумме):
```
2026-01-06 15:30:35 [WARNING] rental - ПРЕДУПРЕЖДЕНИЕ: Создан платёж с крупной суммой | payment_id: 124 | amount: 6000.0
```

## Расширенное тестирование

### Тест 6: Критическая ошибка (опционально)

**ВНИМАНИЕ: Это сломает функционал! Делайте в тестовой среде!**

1. Временно добавьте в `rental/admin.py` в `PaymentAdmin.save_model`:
   ```python
   def save_model(self, request, obj, form, change):
       raise ValueError("Тестовая критическая ошибка")
   ```

2. Попробуйте создать платёж

3. **Ожидаемый результат:**
   - ❌ Красное сообщение: "Ошибка при сохранении платежа: Тестовая критическая ошибка"
   - ❌ Платёж НЕ создан
   - ✅ Запись в `errors.log` С ПОЛНЫМ traceback

4. **ВАЖНО:** Удалите этот тестовый код сразу после проверки!

### Тест 7: Middleware (перехват необработанных ошибок)

1. Создайте тестовое представление в `rental/views.py`:
   ```python
   @staff_member_required
   def test_error_view(request):
       raise ValueError("Тест middleware")
   ```

2. Добавьте в `core/urls.py`:
   ```python
   path('test-error/', views.test_error_view),
   ```

3. Откройте: http://127.0.0.1:8000/test-error/

4. **Ожидаемый результат:**
   - 🎨 Страница 500.html
   - ❌ Сообщение через Django messages (для админов показывает детали)
   - ✅ Запись в `errors.log` с пометкой "НЕОБРАБОТАННОЕ ИСКЛЮЧЕНИЕ"

5. **ВАЖНО:** Удалите тестовый код после проверки!

## Проверка кастомных страниц

### 500.html:
- Красивый дизайн с градиентом
- Иконка 😟
- Понятное объяснение
- Кнопка "Вернуться на главную"
- Информация о поддержке
- Код ошибки

### 404.html:
- Красивый дизайн с градиентом
- Иконка 🔍
- Список что можно сделать
- Кнопки "На главную" и "Назад"
- Код ошибки

### 403.html:
- Красивый дизайн с градиентом
- Иконка 🚫
- Объяснение причин
- Кнопка "Вернуться на главную"
- Код ошибки

## Чеклист успешного тестирования

- [ ] Успешное создание объекта показывает зелёное сообщение
- [ ] Успешное действие логируется в `actions.log`
- [ ] Ошибка валидации показывает красное сообщение с деталями
- [ ] Ошибка валидации логируется в `errors.log` без traceback
- [ ] Крупная сумма показывает успех + предупреждение в логах
- [ ] Страница 404 красиво оформлена
- [ ] Страница 403 красиво оформлена
- [ ] Middleware перехватывает необработанные ошибки
- [ ] Админы видят детали ошибок
- [ ] Логи содержат всю нужную информацию

## Советы по отладке

### Если сообщения не показываются:

1. Проверьте, что в шаблоне есть блок для messages:
   ```django
   {% if messages %}
       {% for message in messages %}
           <div class="alert alert-{{ message.tags }}">
               {{ message }}
           </div>
       {% endfor %}
   {% endif %}
   ```

2. Убедитесь, что middleware правильно настроен в `settings.py`

3. Проверьте, что используете `request` в функциях логирования

### Если логи не пишутся:

1. Проверьте права доступа к папке `logs/`
2. Убедитесь, что папка существует
3. Проверьте настройки LOGGING в `settings.py`
4. Перезапустите сервер

### Если страницы ошибок не показываются:

1. В development (DEBUG=True) Django показывает детальную страницу
2. Для тестирования кастомных страниц установите `DEBUG=False` (временно!)
3. Не забудьте вернуть `DEBUG=True` после тестирования!

## Заключение

После успешного тестирования вы должны увидеть:
- ✅ Дружелюбные сообщения для всех действий
- ✅ Детальные логи всех событий
- ✅ Красивые страницы ошибок
- ✅ Автоматическую обработку большинства ошибок

Система готова к использованию! 🎉

Для дополнительной информации см.:
- `ERROR_HANDLING_GUIDE.md` - полное руководство
- `LOGGING_GUIDE.md` - система логирования
- `rental/admin.py` - примеры в коде

